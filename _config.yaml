### Basic settings ---
# Delete "#" before elements to include

# site properties and page defaults
title: S2O Data Standards
subtitle: 
description: An engaging 1-3 sentence description of your lab.
#logo: images/s2o_logo.svg #This is set by the "_includes/header.html" file
header: images/7628.jpg
footer: images/s2o_background.jpg
proofer: false

# site social media and other links
links:
  email: DataStandards@rti.org
  github: https://github.com/S2O-datastandards
  #twitter: 
  #youtube: 

### jekyll settings

# front matter defaults - default parameters for markdown files
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
  - create_pub_thumbnail.md
